<div class="center jumbotron">
  <h1>Ian Kropp</h1>
  <h2>Software Developer</h2>
</div>


<h3><%= link_to 'New Article', new_article_path %></h3>



<% @articles.each do |art|  %>

  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title"><%= link_to art.title, article_path(art) %></h3>
    </div>
    <div class="panel-body">
      <%= art.text %>    
    </div>
  </div>

    <!--<tr>
      <td><%= art.title %></td>
      <td><%= art.text %></td>
      <td><%= link_to 'Show', article_path(art) %></td>
      <td><%= link_to 'Edit', edit_article_path(art) %></td>
      <td><%= link_to 'Delete', article_path(art), method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>-->
  <% end %>

