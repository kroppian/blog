<%= form_for @user do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>Error! <%= pluralize(@user.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul><% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %></ul>
    </div>
  <% end %>

  <p>
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.submit %>
  </p>

 
<% end %>

